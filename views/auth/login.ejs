<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<link rel="stylesheet" href="/css/style.css">
<body>
    <h1 class="titles">LOGIN ACCOUNT</h1>
   
    <form action="/login" method="post" class="formbox">
        <input hidden value="<%= csrfToken %>" name="_csrf">

        <% if(err) { %>
            <p class="errormsg"><%= err %></p>
            <% } %>


        <div class="filldata">
    <label for="email">email</label>
        <input  id="email" type="text" name="email" value="<%= oldformData.email %>" >
    </div>
    <div class="filldata">
        <label for="password">password
        </label>
            <input  id="psssword" type="text" name="password" value="<%= oldformData.password %>">
        </div>
        
        <button>login</button>
    </form>
     <a href="/resetPassword" class="forget">Forget password</a>
        <a href="/" class="backhome">back home</a>
   
</body>
</html>